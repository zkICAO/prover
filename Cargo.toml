[package]
name = "zkicao-prover"
version = "0.0.1"
edition = "2021"
description = "Off-chain verification library for zkICAO proof bundles, over the Barretenberg CLI"
license = "MIT"

[lib]
name = "zkicao_prover"
path = "src/lib.rs"
