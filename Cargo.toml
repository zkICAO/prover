[package]
name = "zkicao-prover"
version = "0.0.1"
edition = "2021"
description = "Proving and off-chain verification library for zkICAO circuits (noir_rs + native Barretenberg FFI)"
license = "MIT"

[lib]
name = "zkicao_prover"
path = "src/lib.rs"
